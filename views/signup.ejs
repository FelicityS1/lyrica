<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>SignUp - Lyrica</title>
    <link rel="stylesheet" href="logins.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script> <!-- Toast Notification -->
</head>

<body>
    <div class="container- px-0">
        <!-- Header Section -->
        <div class="header">
            <div class="header__title-search">
                <h1>LYRICA</h1>
            </div>
        </div>

        <div class="form-container">
            <h2>SignUp</h2>
            <form id="signupForm">
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username" placeholder="Enter your username" required autocomplete="off">
                </div>
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <button type="submit" class="submit-btn">SignUp</button>
            </form>
            <p>Already have an account? <a href="/login">Log In</a></p>
        </div>
    </div>

    <!-- Toast Notification & Form Submission Script -->
    <script>
        document.getElementById("signupForm").addEventListener("submit", async function(event) {
            event.preventDefault(); // Prevent form from reloading the page
    
            const username = document.getElementById("username").value;
            const password = document.getElementById("password").value;
    
            try {
                const response = await fetch("/signup", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ username, password })
                });
    
                const result = await response.json();
    
                Swal.fire({
                    position: "center",
                    icon: result.success ? "success" : "error",
                    title: result.success ? "User Created" : result.message,
                    showConfirmButton: false,
                    timer: 2000,
                    width: "200px", // **Smaller size**
                    padding: "10px", // **Compact padding**
                    backdrop: "rgba(0, 0, 0, 0.3)", // **Keeps focus on popup without moving the form**
                    customClass: {
                        popup: "small-toast-popup"
                    }
                }).then(() => {
                    if (result.success) window.location.href = "/login"; // Redirect on success
                });
    
            } catch (error) {
                Swal.fire({
                    position: "center",
                    icon: "error",
                    title: "Something went wrong",
                    showConfirmButton: false,
                    timer: 2000,
                    width: "200px", // **Smaller size**
                    padding: "10px",
                    backdrop: "rgba(0, 0, 0, 0.3)",
                    customClass: {
                        popup: "small-toast-popup"
                    }
                });
            }
        });
    </script>
    <style>
        /* Custom styling for a rectangular toast notification */
        .small-toast-popup {
            border-radius: 8px; /* Slightly rounded but rectangular */
            height: 50px; /* Keep it horizontally stretched */
            font-size: 11px; /* Make text smaller */
        }
    </style>
    
</body>
</html>
