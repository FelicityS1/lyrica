<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Music Feed - Lyrica</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Kalnia&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@600&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400&display=swap" rel="stylesheet">

</head>

<body>

    <div class="container- px-0">
        
        <!-- Header Section -->
        <div class="header">
            <div class="header__title-search">
                <h1>LYRICA</h1>
            </div>
            <% if (users && users.name) { %>
                <div class="header__user-section">
                    <span class="header__welcome">Welcome, <%= users.name %>!</span>
                    <a href="/logout" class="header__logout">Logout</a>
                </div>
            <% } else { %>
                <a href="/login" class="header__logout">Login</a>
            <% } %>
        </div>

        <!-- Navigation Bar -->
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar">
                        <li class="<%= activePage === 'home' ? 'active' : '' %>">
                            <a href="/home">HOME</a>
                        </li>
                        <li class="<%= activePage === 'feedback' ? 'active' : '' %>">
                            <a href="/feedback">FEEDBACK</a>
                        </li>
                        <li class="<%= activePage === 'about' ? 'active' : '' %>">
                            <a href="/about">ABOUT</a>
                        </li>
                        <li class="<%= activePage === 'addsongs' ? 'active' : '' %>">
                            <a href="/addsongs">ADD SONGS</a>
                        </li>
                        <li class="<%= activePage === 'musicfeed' ? 'active' : '' %>">
                            <a href="/musicfeed">MUSIC FEED</a>
                        </li>
                    </ul>

                    <form id="searchForm" class="d-flex">
                        <input id="searchInput" class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    </form>
                    <div id="liveSearchResults" class="live-search-results"></div>                    
                    
                </div>
            </div>
        </nav>

        <!-- Music Section -->
        <div class="musicfeed-container">
            <h2>SONG LIST</h2>

            <ul class="musicfeed-list">
                <% if (songs.length > 0) { %>
                    <% songs.forEach(song => { %>
                        <li class="musicfeed-item">
                            <div class="song-details">
                                <div>
                                    <a href="/song/<%= song._id %>"><%= song.title %> - <%= song.artist %></a>
                                    <p class="submitted-by">Submitted by: <%= song.submittedBy || "Anonymous" %></p>
                                </div>
                                <div class="button-group">
                                    <% if (users && users.name === song.submittedBy) { %>
                                        <a href="/updatesong/<%= song._id %>" class="update-button">Update</a>
                                        <form class="delete-form" action="/deletesong/<%= song._id %>" method="POST">
                                            <button type="submit" class="delete-button">Delete</button>
                                        </form>
                                    <% } %>
                                </div>
                                
                            </div>
                        </li>
                    <% }) %>
                <% } else { %>
                    <p class="musicfeed-empty">No songs have been submitted yet.</p>
                <% } %>
            </ul>
            
        
        <!-- Footer Section -->
        <footer class="footer">
            <div class="footer__container">
                <div class="footer__section footer__section--left">
                    <h3 class="footer__heading footer__heading--brand">LYRICA</h3>
                    <p class="footer__text">&copy; 2024 Lyrica. All Rights Reserved.</p>
                </div>

                <div class="footer__section footer__section--center">
                    <ul class="footer__nav">
                        <li class="footer__nav-item"><a href="#" id="contactLink" class="footer__nav-link">Contact</a></li>
                        <li class="footer__nav-item"><a href="#" class="footer__nav-link">Privacy Policy</a></li>
                        <li class="footer__nav-item"><a href="#" class="footer__nav-link">Terms of Service</a></li>
                    </ul>
                </div>
            </div>
        </footer>

        <!-- Modal Container -->
        <div id="modal-container"></div>
        <script src="script.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
            crossorigin="anonymous"></script>

            <script>
                document.addEventListener("DOMContentLoaded", () => {
                    document.querySelectorAll(".delete-form").forEach(form => {
                        form.addEventListener("submit", (event) => {
                            if (!confirm("Are you sure you want to delete this song?")) {
                                event.preventDefault(); 
                            }
                        });
                    });
                });
            </script>
            

</body>
</html>
